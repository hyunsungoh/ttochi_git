#include <stdio.h>
#include <stdlib.h>

#define NAME_LEN    20
#define PHONE_LEN   20


typedef struct TEL
{
    char name[NAME_LEN];
    char phone[PHONE_LEN];
}TEL;

void FindOp()
{
    for(i=0;i=<100;i++)
    {
        if (name[i]>=10000 && name[i]<=99999)
        {
            
        }

        if (name[i] = -1)
        {
            
        }
    }
}